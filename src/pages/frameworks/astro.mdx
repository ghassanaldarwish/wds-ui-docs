---
layout: ../../layouts/index.astro
title: "astro"
path: "/frameworks/astro"
description: "Install and configure Astro. "
tags: ["astro", "component"]
---

import { Steps, Step } from "../../components/steps";
import CodePreview from "../../components/code-preview";

import PackagesManagerShell from "../../components/packages-manager-shell";

<Steps client:idle >

# Astro

Install and configure Astro.

To use @wds-ui in your project, you need to follow the following steps:

<Step client:idle >
## Create project

</Step>

Start by creating a new React project using astro:

<PackagesManagerShell
  client:idle
  install={{
    npm: "npm create astro@latest",
    yarn: "yarn create astro",
    pnpm: "pnpm create astro@latest",
  }}
/>

<Step step={2} client:idle >
## Add Tailwind and its configuration

</Step>

Install tailwindcss and its peer dependencies and plugins, then generate your `tailwind.config.js` and `postcss.config.js` files:

<PackagesManagerShell
  client:idle
  install={{
    npm: "npm install -D tailwindcss postcss autoprefixer tailwindcss-animate @tailwindcss/typography @tailwindcss/aspect-ratio && npx tailwindcss init -p",
    yarn: "yarn add -D tailwindcss postcss autoprefixer tailwindcss-animate @tailwindcss/typography @tailwindcss/aspect-ratio && npx tailwindcss init -p",
    pnpm: "pnpm add -D tailwindcss postcss autoprefixer tailwindcss-animate @tailwindcss/typography @tailwindcss/aspect-ratio && npx tailwindcss init -p",
  }}
/>

<CodePreview code={
`
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./src/**/*.{astro,html,js,jsx,md,mdx,svelte,ts,tsx,vue}",
    "./node_modules/@wds-ui/**/*.{js,jsx,ts,tsx,html}",
  ],
   darkMode: "class",
  theme: {
    container: {
      center: true,
      padding: "2rem",
      screens: {
        "2xl": "1400px",
      },
    },
    extend: {
      gridTemplateRows: {
        // Simple 16 row grid
        12: "repeat(12, minmax(0, 1fr))",
      },
      gridTemplateColumns: {
        // Simple 16 column grid
        16: "repeat(16, minmax(0, 1fr))",
      },
      colors: {
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        ring: "hsl(var(--ring))",
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        primary: {
          DEFAULT: "hsl(var(--primary))",
          foreground: "hsl(var(--primary-foreground))",
        },
        secondary: {
          DEFAULT: "hsl(var(--secondary))",
          foreground: "hsl(var(--secondary-foreground))",
        },
        destructive: {
          DEFAULT: "hsl(var(--destructive))",
          foreground: "hsl(var(--destructive-foreground))",
        },
        muted: {
          DEFAULT: "hsl(var(--muted))",
          foreground: "hsl(var(--muted-foreground))",
        },
        accent: {
          DEFAULT: "hsl(var(--accent))",
          foreground: "hsl(var(--accent-foreground))",
        },
        popover: {
          DEFAULT: "hsl(var(--popover))",
          foreground: "hsl(var(--popover-foreground))",
        },
        card: {
          DEFAULT: "hsl(var(--card))",
          foreground: "hsl(var(--card-foreground))",
        },
      },
      borderRadius: {
        lg: "var(--radius)",
        md: "calc(var(--radius) - 2px)",
        sm: "calc(var(--radius) - 4px)",
      },
      keyframes: {
        "accordion-down": {
          from: { height: 0 },
          to: { height: "var(--radix-accordion-content-height)" },
        },
        "accordion-up": {
          from: { height: "var(--radix-accordion-content-height)" },
          to: { height: 0 },
        },
      },
      animation: {
        "accordion-down": "accordion-down 0.2s ease-out",
        "accordion-up": "accordion-up 0.2s ease-out",
      },
    },
  },
  plugins: [
    require("tailwindcss-animate"),
    require("@tailwindcss/typography"),
    require("@tailwindcss/aspect-ratio"),
  ],
};

`

} client:idle />

<Step step={3} client:idle >
## Style System

</Step>

Easily improve your app's look with the @wds-ui style system. It gives you full control over themes, making your app look great and professional.

**Add the following Tailwind directives and rules to your CSS (src/style.css)**

<CodePreview client:idle

language='css'

code={`

@tailwind base;
@tailwind components;
@tailwind utilities;

@layer utilities {
.prose {
@apply text-foreground;
--tw-prose-body: var(--foreground);
--tw-prose-headings: var(--foreground);

    --tw-prose-links: var(--primary);
    --tw-prose-bold: var(--foreground);
    --tw-prose-counters: var(--foreground);
    --tw-prose-bullets: var(--foreground);
    --tw-prose-hr: var(--border);
    --tw-prose-quotes: var(--foreground);
    --tw-prose-quote-borders: var(--primary);
    --tw-prose-captions: var(--muted-foreground);
    --tw-prose-code: var(--foreground);
    --tw-prose-pre-code: var(--foreground);
    --tw-prose-pre-bg: var(--muted);
    --tw-prose-th-borders: var(--border);
    --tw-prose-td-borders: var(--border);
    --tw-prose-invert-body: var(--card-foreground);
    --tw-prose-invert-headings: var(--card-foreground);
    --tw-prose-invert-links: var(--primary-foreground);
    --tw-prose-invert-bold: var(--card-foreground);
    --tw-prose-invert-counters: var(--card-foreground);
    --tw-prose-invert-bullets: var(--card-foreground);
    --tw-prose-invert-hr: var(--border);
    --tw-prose-invert-quotes: var(--card-foreground);
    --tw-prose-invert-quote-borders: var(--primary-foreground);
    --tw-prose-invert-captions: var(--muted-foreground);
    --tw-prose-invert-code: var(--card-foreground);
    --tw-prose-invert-pre-code: var(--card-foreground);
    --tw-prose-invert-pre-bg: var(--muted);
    --tw-prose-invert-th-borders: var(--border);
    --tw-prose-invert-td-borders: var(--border);

}
}
@layer base {
:root {
--background: 0 0% 100%;
--foreground: 220 9.4% 12.5%;
--card: 0 0% 100%;
--card-foreground: 70 8.1% 12%;
--popover: 0 0% 100%;
--popover-foreground: 70 8.1% 12%;
--primary: 212 100% 38.6%;
--primary-foreground: 0 0% 100%;
--secondary: 210 29% 97%;
--secondary-foreground: 70 8.1% 12%;
--muted: 0 0% 95%;
--muted-foreground: 70 8.1% 50%;
--accent: 0 0% 95%;
--accent-foreground: 70 8.1% 12%;
--destructive: 359 100% 43.7%;
--destructive-foreground: 0 0% 100%;
--border: 219 13.5% 89%;
--input: 219 13.5% 71%;
--ring: 212 100% 38.6%;
--radius: 0.5rem;
--live: 120 100% 27.8%;
}

.dark {
--background: 70 8.1% 12%;
--foreground: 60 36.4% 95.7%;
--card: 70 8.1% 14.5%;
--card-foreground: 60 36.4% 95.7%;
--popover: 70 8.1% 14.5%;
--popover-foreground: 60 36.4% 95.7%;
--primary: 212 100% 38.6%;
--secondary: 70 8.1% 7%;
--secondary-foreground: 60 36.4% 95.7%;
--muted: 70 8.1% 14.5%;
--muted-foreground: 70 8.1% 45%;
--accent: 70 8.1% 18%;
--accent-foreground: 60 36.4% 95.7%;
--destructive: 359 100% 43.7%;
--destructive-foreground: 60 36.4% 95.7%;
--border: 222 13.9% 28.2%;
--input: 219 13.5% 71%;
--ring: 224.3 76.3% 48%;
--live: 120 100% 27.8%;
}
}

@layer base {

\*{
@apply border-border;
}
body {
@apply bg-background text-foreground;
}

html {
font-family: "Inter", sans-serif;
}
}
`}

/>

<Step step={4} client:idle >
## Theming

</Step>

**Add the following script in the end of your layout file:**

<CodePreview client:idle

language='css'
code={`

<script is:inline>
	const getThemePreference = () => {
		if (typeof localStorage !== 'undefined' && localStorage.getItem('theme')) {
			return localStorage.getItem('theme');
		}
		return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
	};
	const isDark = getThemePreference() === 'dark';
	document.documentElement.classList[isDark ? 'add' : 'remove']('dark');
 
	if (typeof localStorage !== 'undefined') {
		const observer = new MutationObserver(() => {
			const isDark = document.documentElement.classList.contains('dark');
			localStorage.setItem('theme', isDark ? 'dark' : 'light');
		});
		observer.observe(document.documentElement, { attributes: true, attributeFilter: ['class'] });
	}
  <script >
`}

/>

<Step step={5} client:idle >
## Install @astrojs/react

</Step>


Run the following from your project directory and follow the prompts:

<PackagesManagerShell
  client:idle
  install={{
    npm: "npx astro add react",
    yarn: "yarn astro add react",
    pnpm: "pnpm astro add react",
  }}
/>


<Step step={6} client:idle >
## That's it

</Step>

You can now use @wds-ui components in your project.

import { ComponentPreview } from "../../components/component-preview";

<ComponentPreview name="theme-toggle" client:idle />



[more react components](/components/button)

</Steps>
























